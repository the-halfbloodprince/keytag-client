class User {
  String name, mobile, email, room, roll, image;
  User({
    required this.name,
    required this.mobile,
    required this.email,
    required this.room,
    required this.roll,
    required this.image,
  });
}
